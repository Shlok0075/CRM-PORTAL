Admin app (React + Vite + Tailwind + shadcn)

Commands:
- pnpm --filter admin install
- pnpm --filter admin dev

This is a minimal scaffold. Install the shadcn UI and Tailwind components per their docs to continue UI work.
